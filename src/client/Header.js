import React, { Component } from 'react'
import { Heading, Box, Columns, Column } from 'fannypack'

const Diffs = () => (
  <Columns padding="0.5rem">
    <Column>
      <Box backgroundColor="whitesmoke" padding="0.5rem">
        <Heading>Diff Generator</Heading>
      </Box>
    </Column>
  </Columns>
)

export default Diffs
